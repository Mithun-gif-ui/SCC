{
  "name": "scc-monorepo",
  "version": "1.0.0",
  "description": "Smart Campus Companion - Monorepo",
  "scripts": {
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "seed:api": "npm run seed:api --prefix backend",
    "seed:groups": "npm run seed:groups --prefix backend"
  },
  "dependencies": {
    "react-hot-toast": "^2.6.0",
    "tesseract.js": "^7.0.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
